@*@using CinemaApp.Domain.Entities
@using CinemaApp.WebUI.Models
@model Movie

<div class="row">
    <div class="col-md-2">
        <img src="~/Content/Images/@Model.ImageData" width="100" height="146" alt="@Model.movie.Name" style="margin-top:20px">
    </div>
    <div class="col-md-8">
        <h3>
            @Model.Name
            <span>
                @foreach (char k in Model.Kijkwijzer)
                {
                    foreach (var kijkwijzer in Model.Kijkwijzer)
                    {
                        if (k.ToString() == kijkwijzer.Character)
                        {
                            <img src="~/Content/Images/Icons/@kijkwijzer.ImageData" width="30" height="30" alt="@kijkwijzer.Name"/>
                        }
                    }
                }
            </span>
        </h3>
        <p id="description">
            @Model.Movie.Description
        </p>
        <a href=@Url.Action("MovieDetails", "Movie", Model)>
            Meer informatie..
        </a>
        <br />

        @foreach (var schedule in Model.Schedules.OrderBy(x => x.Date))
        {
            if (schedule.movie.Id == Model.Movie.Id)
            {
                <div class="btn-group" style="margin-top: 10px">
                    <a href="#" class="btn btn-default">@schedule.Date.ToString("HH:mm") | @schedule.hall.Name</a>
                    @if (@Model.Movie.type != null)
                    {
                        <a class="btn btn-default disabled">@Model.Movie.type</a>
                    }
                </div>
            }
        }
    </div>
    <div class="clearfix visible-md-block"></div>
</div>


$("#description").dotdotdot();*@